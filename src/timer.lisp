(in-package #:hipocrite)

(defun call-after (time function)
  ;; TODO
  )
(defun cancel-timer (timer-ref)
  ;; TODO
  )
(defun send-after (time message &optional (actor (current-actor)))
  ;; TODO
  )
(defun send-interval (time message &optional (actor (current-actor)))
  ;; TODO
  )
(defun exit-after (time reason &optional (actor (current-actor)))
  ;; TODO
  )
(defun kill-after (time reason &optional (actor (current-actor)))
  ;; TODO
  )
